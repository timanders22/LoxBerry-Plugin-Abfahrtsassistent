# Was eingecheckt ist, wird auch ausgeliefert. Keine Umschreibung.
#
# ANLASS: MESSUNG AM GERAET, 06.09.2026
#
# Die Fassung 1.6.7 wurde am 05.09.2026 auf dem LoxBerry installiert. Von
# fuenf nachgemessenen Dateien stimmten die beiden LF-Dateien im MD5 mit dem
# freigegebenen Archiv ueberein, die beiden CRLF-Dateien nicht - wohl aber,
# nachdem man das Archiv auf LF normiert hatte:
#
#     abfahrt_lib.php   Archiv 134 642 Byte (2 920 CRLF)  ->  Geraet 131 722
#     index.php         Archiv  85 715 Byte (1 341 CRLF)  ->  Geraet  84 374
#
# Der Weg ueber git und GitHub stellt also um. Inhaltlich schadet das nichts
# - LoxBerry liest diese Dateien zeilenweise, und LF ist das, was jedes
# Linux-Werkzeug erwartet. Es kostet aber die Aussage "der Anwender bekommt
# die Bytes, die gepackt wurden": jeder Vergleich Geraet gegen Archiv ueber
# eine Pruefsumme meldet einen Unterschied, den es inhaltlich nicht gibt, und
# ein rotes Kreuz, das nichts bedeutet, ist schlimmer als keine Pruefung.
#
# "-text" heisst: git rechnet an keiner Datei herum, weder beim Einchecken
# noch beim Auschecken noch beim Erzeugen des Tag-Archivs.
#
# NICHT erledigt: das Hausziel "LF ueberall" (Regeln/03). Sechs Dateien
# dieser Linie fuehren CRLF (README.md, beide language_*.ini, abfahrt_lib.php,
# index.php, termin_say.php). Sie umzustellen ist eine eigene Entscheidung -
# es aendert jede Zeile dieser sechs Dateien und macht den naechsten
# Fassungsvergleich unlesbar. Diese Datei sorgt bis dahin dafuer, dass
# wenigstens ankommt, was gepackt wurde.
*	-text
